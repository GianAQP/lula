package com.aqpseller.lulaapp.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de marca — ver Plan/09-guia-visual.md. Cálida y positiva a propósito: nada de rojo
// de alerta/castigo, cada color tiene un significado fijo y se reutiliza siempre igual.

/** Violeta — color de marca de Lula, usado en acciones primarias y el ícono "Hoy". */
val LulaPrimary = Color(0xFF7C6FF0)
val LulaPrimaryLight = Color(0xFFB8AEFF)
val LulaPrimaryContainerLight = Color(0xFFECE8FF)
val LulaPrimaryContainerDark = Color(0xFF2C2750)

/** Naranja cálido — racha / energía. Nunca rojo de alarma. */
val LulaRacha = Color(0xFFFF9F45)
val LulaRachaContainerLight = Color(0xFFFFEEDD)
val LulaRachaContainerDark = Color(0xFF4A331A)

/** Verde — hábitos, crecimiento, confirmación positiva. */
val LulaHabito = Color(0xFF4CC38A)
val LulaHabitoContainerLight = Color(0xFFDFF6EB)
val LulaHabitoContainerDark = Color(0xFF1E3A2E)

/** Azul — tareas. */
val LulaTarea = Color(0xFF4FA3FF)
val LulaTareaContainerLight = Color(0xFFE1EFFF)
val LulaTareaContainerDark = Color(0xFF1B3350)

/** Dorado — finanzas. */
val LulaFinanzas = Color(0xFFE0A425)
val LulaFinanzasContainerLight = Color(0xFFFCEFD1)
val LulaFinanzasContainerDark = Color(0xFF4A3B15)

/** Rosado — asistente / celebración (cierre de día, logros). */
val LulaAsistente = Color(0xFFEF7BA0)
val LulaAsistenteContainerLight = Color(0xFFFCE4EC)
val LulaAsistenteContainerDark = Color(0xFF4A2030)

/** Turquesa — el único color usado para "estás en un espacio Familia", nunca para otra cosa,
 * para que sea inconfundible en cualquier pantalla (ver feedback del usuario, 2026-07-30). */
val LulaFamilia = Color(0xFF1FA2A6)
val LulaFamiliaContainerLight = Color(0xFFD3F3F2)
val LulaFamiliaContainerDark = Color(0xFF0F3B3C)

val LulaFondoClaro = Color(0xFFFBF9FF)
val LulaSuperficieClaro = Color(0xFFFFFFFF)
val LulaFondoOscuro = Color(0xFF17151F)
val LulaSuperficieOscuro = Color(0xFF211E2C)
