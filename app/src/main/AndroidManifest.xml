<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Mantiene la CPU despierta mientras suena el nivel Alarma — sin esto, con el teléfono
         inactivo (pantalla apagada) el sistema puede volver a dormir a mitad de la reproducción
         y cortar el sonido en menos de un segundo, aunque el Service siga vivo en primer plano.
         Ver `AlarmaSonidoService`, `Plan/08-decisiones-tecnicas.md`. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Solo para pedir la excepción de forma explícita (botón en Ajustes) — Lula nunca la
         asume ni la fuerza en segundo plano. Play Store la permite para apps cuya función
         principal depende de alarmas/recordatorios confiables (ver `08-decisiones-tecnicas.md`). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".LulaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LulaApp">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LulaApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".core.notifications.RecordatorioReceiver"
            android:exported="false" />

        <receiver
            android:name=".core.notifications.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".core.notifications.AlarmaSonidoService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>

</manifest>
